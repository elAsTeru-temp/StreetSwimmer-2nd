using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SharkStatus : MonoBehaviour
{
    public int power = 0;       //サメの攻撃力
    public int speed = 0;       //サメのスピード
    public int health = 0;      //サメの体力
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
